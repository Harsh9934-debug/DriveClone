<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shared File - DataVault</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #000000;
            color: #ffffff;
        }
        .shared-container {
            background-color: #1a1a1a;
            border: 1px solid #333;
        }
        .btn-primary {
            background-color: #2563eb;
            color: white;
            transition: background-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #1d4ed8;
        }
        .btn-secondary {
            background-color: #374151;
            color: white;
            transition: background-color 0.3s ease;
        }
        .btn-secondary:hover {
            background-color: #4b5563;
        }
        .file-info {
            background-color: #2a2a2a;
            border: 1px solid #444;
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center px-4 py-10">
    <div class="w-full max-w-lg mx-auto">
        <div class="shared-container p-8 md:p-12 rounded-2xl shadow-lg">
            <div class="flex items-center justify-between mb-8">
                <div class="flex items-center">
                    <div class="w-10 h-10 bg-blue-600 rounded-xl flex items-center justify-center mr-3">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path></svg>
                    </div>
                    <span class="text-xl font-bold text-white">DataVault</span>
                </div>
                
                <% if (typeof shareLink.oneTimeUse !== 'undefined' && shareLink.oneTimeUse) { %>
                    <span class="px-3 py-1 bg-yellow-900/50 border border-yellow-700/50 rounded-full text-xs font-medium text-yellow-400">
                        One-time access
                    </span>
                <% } else { %>
                    <span class="px-3 py-1 bg-blue-900/50 border border-blue-700/50 rounded-full text-xs font-medium text-blue-400">
                        Shared file
                    </span>
                <% } %>
            </div>
            
            <div class="text-center mb-8">
                <div class="mb-4 flex justify-center">
                    <div class="w-16 h-16 flex items-center justify-center text-4xl">
                        <% 
                        const mimetype = file && typeof file.mimetype === 'string' ? file.mimetype : '';
                        if (mimetype.startsWith('image/')) { %>
                            🖼️
                        <% } else if (mimetype.includes('pdf')) { %>
                            📄
                        <% } else if (mimetype.includes('word') || mimetype.includes('document')) { %>
                            📝
                        <% } else if (mimetype.includes('spreadsheet') || mimetype.includes('excel')) { %>
                            📊
                        <% } else if (mimetype.includes('zip') || mimetype.includes('rar')) { %>
                            📦
                        <% } else { %>
                            📄
                        <% } %>
                    </div>
                </div>
                
                <h1 class="text-3xl font-bold text-white mb-2"><%= file.originalName %></h1>
                
                <p class="text-gray-400 text-sm mb-4">
                    <% 
                    let fileSize = "Unknown";
                    if (typeof file.size === 'number') {
                        fileSize = (file.size / 1024 / 1024).toFixed(2) + " MB";
                    }
                    %>
                    <%= fileSize %> • 
                    Shared by <%= typeof file.uploadedBy === 'object' && file.uploadedBy && file.uploadedBy.name ? file.uploadedBy.name : 'Unknown' %> •
                    <% if (typeof file.description === 'string' && file.description) { %>
                        <%= file.description %>
                    <% } else { %>
                        No description provided
                    <% } %>
                </p>
                
                <div class="w-full bg-gray-800 h-1 rounded-full overflow-hidden mb-2">
                    <% 
                        // Calculate expiration progress (0-100)
                        let progressPercent = 0;
                        try {
                            const now = new Date();
                            const created = new Date(shareLink.createdAt || now);
                            const expires = new Date(shareLink.expiresAt);
                            const total = Math.max(1, expires - created); // Avoid division by zero
                            const elapsed = now - created;
                            progressPercent = Math.min(100, Math.max(0, (elapsed / total) * 100));
                        } catch (e) {
                            progressPercent = 0;
                        }
                    %>
                    <script>
                        document.addEventListener('DOMContentLoaded', function() {
                            document.getElementById('progress-bar').style.width = '<%= progressPercent %>%';
                        });
                    </script>
                    <div id="progress-bar" class="bg-blue-600 h-full w-0 transition-all duration-1000"></div>
                </div>
                
                <p class="text-xs text-gray-400">
                    <% if (typeof shareLink.oneTimeUse !== 'undefined' && shareLink.oneTimeUse && 
                           typeof shareLink.accessCount !== 'undefined' && shareLink.accessCount < 1) { %>
                        This is a one-time use link that will expire after you download the file.
                    <% } else { %>
                        Link expires on <%= new Date(shareLink.expiresAt).toLocaleString() %>
                    <% } %>
                </p>
            </div>
            
            <div class="text-center space-y-4">
                <a href="/s/<%= shareLink.token %>/download" class="btn-primary w-full py-4 px-6 rounded-lg font-medium flex items-center justify-center">
                    <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                    Download File
                </a>
                
                <a href="/" class="btn-secondary w-full py-3 px-6 rounded-lg font-medium block">
                    Go to DataVault Home
                </a>
            </div>
            
            <div class="mt-8 pt-6 border-t border-gray-800 text-center">
                <p class="text-xs text-gray-500">
                    This link was shared securely via DataVault's time-limited sharing feature.
                    <% if (typeof shareLink.oneTimeUse !== 'undefined' && shareLink.oneTimeUse) { %>
                        This link can only be used once.
                    <% } %>
                </p>
            </div>
        </div>
    </div>
</body>
</html>